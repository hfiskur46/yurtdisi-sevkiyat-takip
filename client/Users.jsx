import React,{useEffect,useState} from 'react';import axios from 'axios';export default function Users(){ const [users,setUsers]=useState([]); useEffect(()=>{ axios.get('/api/users',{headers:{Authorization:'Bearer '+localStorage.getItem('token')}}).then(r=>setUsers(r.data)).catch(()=>{}); },[]); return (<div className='max-w-7xl mx-auto'><h2 className='text-xl font-semibold mb-4'>Kullanıcılar</h2><div className='grid md:grid-cols-3 gap-4'>{users.map((u,i)=>(<div key={i} className='card'><div className='font-semibold'>{u.name}</div><div className='text-sm text-slate-500'>{u.role}</div><div className='text-sm mt-2'>{u.email}</div></div>))}</div></div>}